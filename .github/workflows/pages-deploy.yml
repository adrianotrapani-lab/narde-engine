      - name: Prepare public folder from static
        run: |
          rm -rf public || true
          mkdir -p public
          if [ -d static ]; then
            cp -a static/. public/
          else
            echo "static directory not found"
          fi
