// Minimal placeholder to satisfy Pages 404 for undo.js.
// Replace with your real built undo.js bundle when ready.
(function(){
  // no-op undo API used as placeholder
  window.undo = {
    register: function(){},
    perform: function(){},
    canUndo: function(){ return false; }
  };
})();
